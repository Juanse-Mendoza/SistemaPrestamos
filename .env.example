# Copiar este archivo como .env y completar los valores reales
# NUNCA subir el archivo .env al repositorio

# Conexión PostgreSQL (contenedor Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=prestamos_umb
DB_USER=app_prestamos
DB_PASSWORD=App_Prestamos_2026!

# Clave secreta para JWT (cambiar por una cadena aleatoria larga en producción)
JWT_SECRET_KEY=cambiar_esta_clave_secreta_en_produccion_2026
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=8
